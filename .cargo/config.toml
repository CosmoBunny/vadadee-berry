# Help opencv-rust bindgen find libclang (Debian/Ubuntu LLVM packages).
# Override in your environment if needed: export LIBCLANG_PATH=...
[env]
LIBCLANG_PATH = { value = "/usr/lib/llvm-18/lib", force = false }
